pub struct Adj {
    // 邻接表
    // let mut adj_list: Vec<Vec<(usize, i32)>> = vec![vec![]; n];
    data: Vec<Vec<(usize, i32)>>,
}

impl Adj {









    pub fn show(&self) {

        for _node_idx in 0..self.data.len() {

        }

    }


    // 要求: 没有负数的权重
    pub fn djs(&self) {}
}
